

## Plan: Remove debug log lines from LogInteractionSheet

**File:** `src/components/LogInteractionSheet.tsx`

Remove the two `console.log("[debug] getConnectDateISO:", ...)` lines from the `logMutation` `mutationFn`. All other `console.log` statements preserved. No other files touched.

